import React from 'react';
import { HomeScreen } from './src/screens/HomeScreen';

// Punto de entrada de la app — renderiza la HomeScreen
export default function App(): React.JSX.Element {
  return <HomeScreen />;
}